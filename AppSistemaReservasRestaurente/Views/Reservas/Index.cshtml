@model IEnumerable<AppSistemaReservasRestaurente.Models.Reserva>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mesa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Horario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cantidad_Personas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente.ID_Cliente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mesa.ID_Mesa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Horario.ID_Horario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fecha)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cantidad_Personas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Reserva">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Reserva">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Reserva">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
